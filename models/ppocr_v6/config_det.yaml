# PP-OCRv6 medium detection, 960x960.
#
# featuremap / no_preprocess matches PP-OCRv5 so the runtime's existing OCR
# preprocessing path works unchanged. The consequence is that the normalisation
# contract lives in calib.py rather than here -- see that file's docstring.
model_parameters:
  onnx_model: "ppocrv6_medium_det_960x960.onnx"
  march: "nash-m"
  output_model_file_prefix: "ppocrv6_medium_det_960x960"
  working_dir: "./out_det"
input_parameters:
  input_name: "x"
  input_shape: "1x3x960x960"
  input_type_train: "featuremap"
  input_type_rt: "featuremap"
  input_layout_train: "NCHW"
  norm_type: "no_preprocess"
calibration_parameters:
  cal_data_dir: "/ws/cal_det"
  calibration_type: "default"
compiler_parameters:
  optimize_level: "O2"
  compile_mode: "latency"
  jobs: 16
