# PP-OCRv6 medium recognition, WIDE + all int16 — 48x960, 120 timesteps.
#
# The accuracy-first long-line build: the 960 width for long lines (see
# config_rec_960.yaml) plus set_all_nodes_int16 for the lower character error
# rate int16 gives on recognition. This is the heaviest rec build here.
#
# Pair the .hbm with ppocr_keys_v6_18710.txt. The tiny size is a DIFFERENT
# charset (6906) and mixing them decodes to confident nonsense.
#
# featuremap / no_preprocess matches PP-OCRv5 so the runtime's existing OCR
# preprocessing path works unchanged. The consequence is that the normalisation
# contract lives in calib.py rather than here -- see that file's docstring.
model_parameters:
  onnx_model: "ppocrv6_medium_rec_48x960.onnx"
  march: "nash-m"
  output_model_file_prefix: "ppocrv6_medium_rec_int16_48x960"
  working_dir: "./out_rec_int16_960"
input_parameters:
  input_name: "x"
  input_shape: "1x3x48x960"
  input_type_train: "featuremap"
  input_type_rt: "featuremap"
  input_layout_train: "NCHW"
  norm_type: "no_preprocess"
calibration_parameters:
  cal_data_dir: "/ws/cal_rec_960"
  calibration_type: "default"
  optimization: "set_all_nodes_int16"
compiler_parameters:
  optimize_level: "O2"
  compile_mode: "latency"
  jobs: 16
